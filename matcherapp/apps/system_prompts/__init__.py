"""
Central LLM system prompts and user templates for RankForge.

- ``scoring`` — two-stage scorer, few-shot, resume gate
- ``extraction`` — JD / resume profile extraction
- ``ontology`` — structured skill extraction from raw text
- ``dimensions`` — D1 / D2 / D3 agent and fallback prompts
"""
